apiVersion: v1
kind: Service
metadata:
  name: secure-app
  namespace: secure-app
spec:
  selector:
    app: secure-app
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
